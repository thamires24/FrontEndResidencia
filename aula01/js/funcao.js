//Função não declarativa

function soma(a,b){
    return a + b;
}

console.log (soma(15,30));

const numeros = [1, 7, 9, 21, 45, 72];

function media(){
    for (let i = 0; i < nums.length; i++) {
        soma += numeros[i];
    }
    return Match.round(soma / nums.length);

    console.log("A média é: " + media(numeros));
}